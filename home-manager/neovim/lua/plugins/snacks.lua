return {
  "folke/snacks.nvim",
  opts = {
    -- Disable indent feature to prevent "Invalid window id" errors
    indent = { enabled = false },
    -- You can also try disabling scope if the error persists
    -- scope = { enabled = false },
  },
}